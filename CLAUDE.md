## SDK Usage

Before writing any code that uses an unfamiliar SDK:
1. Locate the installed package and read its public API surface from `__init__.py`
2. Find an official example in the package README or docs
3. Confirm method names, required args, and auth pattern

Only then write the script.
